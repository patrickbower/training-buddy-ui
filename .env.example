# Copy to .env.local and fill in values

# Set to "true" to use MSW mocks instead of hitting a real API
VITE_USE_MOCKS=true

# API base URL (leave as /api for dev proxy, set full URL for production)
VITE_API_BASE_URL=/api
